git clone https://github.com/RajeshNandhan/CoreAPIAdditional.git

git status

git add .

git status

git commit -m "first commit"

git status

git push origin master

git add .

git pull origin master

git remote -v


------------------------------


git config --global user.name RajeshNandhan

git config --global user.email 
git config --global user.name RajeshNandhan
git config --list
